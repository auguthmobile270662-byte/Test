-- สร้างหน้าจอหลัก (ScreenGui)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "JaRoTabbedGUI"
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ResetOnSpawn = false

-- ปุ่มเปิด/ปิด UI ลอยบนจอ
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleBtn"
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Position = UDim2.new(0, 20, 0.5, -25)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ToggleBtn.TextColor3 = Color3.fromRGB(0, 170, 255)
ToggleBtn.Text = "JaRo"
ToggleBtn.TextSize = 12
ToggleBtn.Font = Enum.Font.Code
ToggleBtn.Parent = ScreenGui

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 8)
ToggleCorner.Parent = ToggleBtn

-- หน้าต่างหลัก (Main Frame)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 580, 0, 360)
MainFrame.Position = UDim2.new(0.5, -290, 0.5, -180)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

-- แถบหัวข้อด้านบน
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "JaRo Hub | Menu System"
Title.TextColor3 = Color3.fromRGB(0, 170, 255)
Title.TextSize = 15
Title.Font = Enum.Font.Code
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

-- ระบบเปิด-ปิดหน้าต่าง UI
local isOpen = true
ToggleBtn.MouseButton1Click:Connect(function()
   isOpen = not isOpen
   MainFrame.Visible = isOpen
end)

-- ================= แถบเมนูด้านซ้าย (Sidebar) =================
local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 140, 0, 305)
Sidebar.Position = UDim2.new(0, 15, 0, 40)
Sidebar.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainFrame

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 8)
SidebarCorner.Parent = Sidebar

-- ฟังก์ชันสร้างปุ่มเลือกแท็บฝั่งซ้าย
local function createTabButton(name, text, positionY)
   local btn = Instance.new("TextButton")
   btn.Name = name
   btn.Size = UDim2.new(1, -16, 0, 40)
   btn.Position = UDim2.new(0, 8, 0, positionY)
   btn.BackgroundColor3 = Color3.fromRGB(32, 32, 32)
   btn.TextColor3 = Color3.fromRGB(0, 150, 255)
   btn.Text = text
   btn.TextSize = 13
   btn.Font = Enum.Font.Code
   btn.Parent = Sidebar

   local corner = Instance.new("UICorner")
   corner.CornerRadius = UDim.new(0, 6)
   corner.Parent = btn
   
   return btn
end

local TabSSPY = createTabButton("TabSSPY", "SSPY", 10)
local TabPlayer = createTabButton("TabPlayer", "Player", 60)

-- ================= พื้นที่แสดงเนื้อหาด้านขวา (Container) =================
local ContentContainer = Instance.new("Frame")
ContentContainer.Size = UDim2.new(0, 395, 0, 305)
ContentContainer.Position = UDim2.new(0, 165, 0, 40)
ContentContainer.BackgroundTransparency = 1
ContentContainer.Parent = MainFrame

-- หน้าที่ 1: SSPY (แบ่งครึ่งซ้าย-ขวา ข้างใน SSPY คือ กล่องโค้ด + ปุ่มฟังก์ชันโค้ด)
local SSPYPage = Instance.new("Frame")
SSPYPage.Size = UDim2.new(1, 0, 1, 0)
SSPYPage.BackgroundTransparency = 1
SSPYPage.Visible = true
SSPYPage.Parent = ContentContainer

-- กล่องข้อความ (TextBox) ใน SSPY
local TextBox = Instance.new("TextBox")
TextBox.Size = UDim2.new(0, 210, 0, 295)
TextBox.Position = UDim2.new(0, 0, 0, 5)
TextBox.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
TextBox.TextColor3 = Color3.fromRGB(0, 190, 255)
TextBox.PlaceholderColor3 = Color3.fromRGB(80, 80, 80)
TextBox.PlaceholderText = "วางโค้ดที่นี่..."
TextBox.Text = ""
TextBox.TextSize = 12
TextBox.Font = Enum.Font.Code
TextBox.ClearTextOnFocus = false
TextBox.MultiLine = true
TextBox.TextXAlignment = Enum.TextXAlignment.Left
TextBox.TextYAlignment = Enum.TextYAlignment.Top
TextBox.Parent = SSPYPage

local BoxCorner = Instance.new("UICorner")
BoxCorner.CornerRadius = UDim.new(0, 6)
BoxCorner.Parent = TextBox

-- เมนูปุ่มฟังก์ชัน SSPY (ScrollingFrame ฝั่งขวาของหน้า SSPY)
local SSPYScroll = Instance.new("ScrollingFrame")
SSPYScroll.Size = UDim2.new(0, 170, 0, 295)
SSPYScroll.Position = UDim2.new(0, 225, 0, 5)
SSPYScroll.BackgroundTransparency = 1
SSPYScroll.BorderSizePixel = 0
SSPYScroll.CanvasSize = UDim2.new(0, 0, 0, 310)
SSPYScroll.ScrollBarThickness = 3
SSPYScroll.Parent = SSPYPage

local function createSSPYButton(text, positionY)
   local btn = Instance.new("TextButton")
   btn.Size = UDim2.new(1, -5, 0, 38)
   btn.Position = UDim2.new(0, 0, 0, positionY)
   btn.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
   btn.TextColor3 = Color3.fromRGB(0, 150, 255)
   btn.Text = text
   btn.TextSize = 12
   btn.Font = Enum.Font.Code
   btn.Parent = SSPYScroll

   local corner = Instance.new("UICorner")
   corner.CornerRadius = UDim.new(0, 6)
   corner.Parent = btn
   return btn
end

local BtnCopy = createSSPYButton("1. คัดลอกโค้ด", 0)
local BtnClear = createSSPYButton("2. เคลียร์โค้ด", 44)
local BtnRunOnce = createSSPYButton("3. รัน 1 ครั้ง", 88)
local BtnRunAuto = createSSPYButton("4. รัวๆ(0.1s): OFF", 132)
local BtnSave = createSSPYButton("5. บันทึกโค้ด", 176)
local BtnHistory1 = createSSPYButton("6. ประวัติบันทึก", 220)
local BtnHistory2 = createSSPYButton("7. ประวัติสำรอง", 264)


-- หน้าที่ 2: Player (ตั้งค่าความเร็วและกระโดด)
local PlayerPage = Instance.new("ScrollingFrame")
PlayerPage.Size = UDim2.new(1, 0, 1, 0)
PlayerPage.Position = UDim2.new(0, 0, 0, 5)
PlayerPage.BackgroundTransparency = 1
PlayerPage.BorderSizePixel = 0
PlayerPage.CanvasSize = UDim2.new(0, 0, 0, 220)
PlayerPage.ScrollBarThickness = 3
PlayerPage.Visible = false
PlayerPage.Parent = ContentContainer

local function createPlayerLabel(text, positionY)
   local lbl = Instance.new("TextLabel")
   lbl.Size = UDim2.new(1, -10, 0, 25)
   lbl.Position = UDim2.new(0, 5, 0, positionY)
   lbl.BackgroundTransparency = 1
   lbl.Text = text
   lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
   lbl.TextSize = 13
   lbl.Font = Enum.Font.Code
   lbl.TextXAlignment = Enum.TextXAlignment.Left
   lbl.Parent = PlayerPage
   return lbl
end

local function createPlayerInput(name, placeholder, positionY)
   local input = Instance.new("TextBox")
   input.Name = name
   input.Size = UDim2.new(1, -10, 0, 35)
   input.Position = UDim2.new(0, 5, 0, positionY)
   input.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
   input.TextColor3 = Color3.fromRGB(0, 255, 150)
   input.PlaceholderColor3 = Color3.fromRGB(100, 100, 100)
   input.PlaceholderText = placeholder
   input.Text = ""
   input.TextSize = 13
   input.Font = Enum.Font.Code
   input.Parent = PlayerPage

   local corner = Instance.new("UICorner")
   corner.CornerRadius = UDim.new(0, 6)
   corner.Parent = input
   return input
end

local function createPlayerButton(name, text, positionY)
   local btn = Instance.new("TextButton")
   btn.Name = name
   btn.Size = UDim2.new(1, -10, 0, 38)
   btn.Position = UDim2.new(0, 5, 0, positionY)
   btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
   btn.TextColor3 = Color3.fromRGB(0, 150, 255)
   btn.Text = text
   btn.TextSize = 13
   btn.Font = Enum.Font.Code
   btn.Parent = PlayerPage

   local corner = Instance.new("UICorner")
   corner.CornerRadius = UDim.new(0, 6)
   corner.Parent = btn
   return btn
end

createPlayerLabel("ความเร็วเคลื่อนที่ (WalkSpeed):", 5)
local SpeedInput = createPlayerInput("SpeedInput", "ใส่ตัวเลขความเร็ว...", 30)
local BtnSpeed = createPlayerButton("BtnSpeed", "ตั้งค่าความเร็ว", 70)

createPlayerLabel("พลังกระโดด (JumpPower):", 115)
local JumpInput = createPlayerInput("JumpInput", "ใส่ตัวเลขกระโดด...", 140)
local BtnJump = createPlayerButton("BtnJump", "ตั้งค่ากระโดด", 180)


-- ================= ระบบสลับหน้าแท็บ (Tab Switching) =================
TabSSPY.MouseButton1Click:Connect(function()
   SSPYPage.Visible = true
   PlayerPage.Visible = false
   TabSSPY.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
   TabPlayer.BackgroundColor3 = Color3.fromRGB(32, 32, 32)
end)

TabPlayer.MouseButton1Click:Connect(function()
   SSPYPage.Visible = false
   PlayerPage.Visible = true
   TabPlayer.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
   TabSSPY.BackgroundColor3 = Color3.fromRGB(32, 32, 32)
end)

-- ตั้งค่าสีเริ่มต้นแท็บ SSPY ให้เด่นขึ้น
TabSSPY.BackgroundColor3 = Color3.fromRGB(45, 45, 45)


-- ================= ฟังก์ชันการทำงานต่างๆ =================
local savedCodeHistory = ""
local autoRunActive = false
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

BtnCopy.MouseButton1Click:Connect(function()
   if setclipboard then
      setclipboard(TextBox.Text)
      BtnCopy.Text = "คัดลอกแล้ว!"
      task.wait(1)
      BtnCopy.Text = "1. คัดลอกโค้ด"
   end
end)

BtnClear.MouseButton1Click:Connect(function()
   TextBox.Text = ""
   BtnClear.Text = "เคลียร์แล้ว!"
   task.wait(1)
   BtnClear.Text = "2. เคลียร์โค้ด"
end)

BtnRunOnce.MouseButton1Click:Connect(function()
   local success = pcall(function()
      local func = loadstring(TextBox.Text)
      if func then func() end
   end)
   if success then
      BtnRunOnce.Text = "รันสำเร็จ!"
   else
      BtnRunOnce.Text = "เกิดข้อผิดพลาด!"
   end
   task.wait(1)
   BtnRunOnce.Text = "3. รัน 1 ครั้ง"
end)

BtnRunAuto.MouseButton1Click:Connect(function()
   autoRunActive = not autoRunActive
   if autoRunActive then
      BtnRunAuto.Text = "รัวๆ: ON"
      BtnRunAuto.TextColor3 = Color3.fromRGB(0, 255, 100)
      task.spawn(function()
         while autoRunActive do
            pcall(function()
               local func = loadstring(TextBox.Text)
               if func then func() end
            end)
            task.wait(0.1)
         end
      end)
   else
      BtnRunAuto.Text = "4. รัวๆ(0.1s): OFF"
      BtnRunAuto.TextColor3 = Color3.fromRGB(0, 150, 255)
   end
end)

BtnSave.MouseButton1Click:Connect(function()
   savedCodeHistory = TextBox.Text
   BtnSave.Text = "บันทึกแล้ว!"
   task.wait(1)
   BtnSave.Text = "5. บันทึกโค้ด"
end)

local function loadHistory()
   if savedCodeHistory ~= "" then
      TextBox.Text = savedCodeHistory
   end
end

BtnHistory1.MouseButton1Click:Connect(function()
   loadHistory()
   BtnHistory1.Text = "โหลดแล้ว!"
   task.wait(1)
   BtnHistory1.Text = "6. ประวัติบันทึก"
end)

BtnHistory2.MouseButton1Click:Connect(function()
   loadHistory()
   BtnHistory2.Text = "โหลดแล้ว!"
   task.wait(1)
   BtnHistory2.Text = "7. ประวัติสำรอง"
end)

BtnSpeed.MouseButton1Click:Connect(function()
   local val = tonumber(SpeedInput.Text)
   if val then
      local char = LocalPlayer.Character
      if char and char:FindFirstChildOfClass("Humanoid") then
         char:FindFirstChildOfClass("Humanoid").WalkSpeed = val
         BtnSpeed.Text = "ตั้งค่าสำเร็จ!"
         task.wait(1)
         BtnSpeed.Text = "ตั้งค่าความเร็ว"
      end
   end
end)

BtnJump.MouseButton1Click:Connect(function()
   local val = tonumber(JumpInput.Text)
   if val then
      local char = LocalPlayer.Character
      if char and char:FindFirstChildOfClass("Humanoid") then
         local hum = char:FindFirstChildOfClass("Humanoid")
         hum.UseJumpPower = true
         hum.JumpPower = val
         BtnJump.Text = "ตั้งค่าสำเร็จ!"
         task.wait(1)
         BtnJump.Text = "ตั้งค่ากระโดด"
      end
   end
end)
